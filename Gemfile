source 'https://rubygems.org'

# Specify your gem's dependencies in adl15parser-ruby.gemspec
require 'rbconfig'
if RbConfig::CONFIG['target_os'] =~ /(?i-mx:bsd|dragonfly)/
  gem 'rb-kqueue', '>= 0.2'
end

gemspec
